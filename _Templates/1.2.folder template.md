<%*
let projectTitle = await tp.system.prompt("Project Title");
await tp.file.create_folder(projectTitle);
await tp.file.create_folder(projectTitle + "/01 Resources");
await tp.file.create_folder(projectTitle + "/02 Notes");
await tp.file.rename(projectTitle);
-%>
