example safe guard:
```python

def safe_contains(items, target):
    if not isinstance(items, list):
        return False
    return target in items

```
always safe guard against improper user inputs
this will protect your code against bad input and keep code from breaking